venv/
.venv/
__pycache__/
*.pyc
*.pyo
.env
.DS_Store
.pytest_cache/
.coverage
dumps/
instance/
config/client_secret.json

# Output do build da SPA SvelteKit (regenerar com: cd frontend && npm run build).
# A fonte fica em frontend/; este diretorio e gerado e nao deve ser versionado.
static/spa/

# Segredos e dumps de banco — NUNCA versionar (auditoria de seguranca 2026-06).
# client_secret.json na raiz, dumps SQL e arquivos SQLite (instance/ ja coberto acima).
/client_secret.json
*.sql
*.db

# Config/sessao do Claude Code, documentacao interna e instrucoes — fora do repo publico.
.claude/
docs/
CLAUDE.md

# Arquivos temporarios de depuracao/probe na raiz (ex.: tmp_*_probe.html).
/tmp_*

node_modules/
